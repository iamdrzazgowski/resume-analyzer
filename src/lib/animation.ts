export const REVEAL_DELAY_MS = {
    badge: 80,
    heading: 180,
    lead: 300,
    visual: 360,
    actions: 420,
    footnote: 520,
} as const;

export const SCAN_LINE_REVEAL_BASE_MS = 300;
export const SCAN_LINE_REVEAL_STRIDE_MS = 320;
